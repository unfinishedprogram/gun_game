# Gun Game

## Building

the client and server can both be built using:

```bash
npm run build
```
or
```bash
bash build.sh
```

## Running the server 

the server can be ran using:

```bash
bash startserver.sh
```